// import { testPaginationRoutes } from '../../helper'

/** 
 * This is for when users sign into the dashboard and want to see what 
 * tickets have been assigned to them
 */
describe('Tickets Assigned To Users', () => {
  describe('GET /users/assigned-tickets', () => {
    beforeAll(async () => {
      // await testPaginationRoutes()
    })
    it('should 401 when the user is not signed in')
  })
})