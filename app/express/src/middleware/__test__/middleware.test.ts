describe('Middleware tests', () => {
  describe('isAuthenticated()', () => {
    test('Should call next() if the user has a valid session', () => {})
    test('', () => {})
  })

  describe('isAuthorized()', () => {

  })
})
