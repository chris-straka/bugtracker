describe('isAuthorized()', () => {
  test('When the user has the correct role, they should get passed onto the next middleware', async () => {

  })

  test('When the user has the wrong role, they should get a ')
})
