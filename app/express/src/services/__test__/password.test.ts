describe('Password service', () => {
  test('toHash(string) produces a valid hash', () => {})
  test('compare(suppliedPassword, givenPassword) fails when given passwords that don\'t match', () => {})
  test('compare(suppliedPassword, givenPassword) succeeds when given passwords that do match', () => {})
})
